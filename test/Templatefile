enum Keys {
    <% @categories.each do |category| %>
        enum <%= category.id %> {
            <% category.items.each do |item| %>
                static let <%= item.property %> = <%= item.value %>
            <% end %>
        }
    <% end %>
}
