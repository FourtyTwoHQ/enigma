enum Keys {
    <% @categories.each do |category| %>
        enum <%= category.id %> {
            <% category.items.each do |item| %>
                static let <%= item.property_name %> = <%= item.value %>
            <% end %>
        }
    <% end %>
}
